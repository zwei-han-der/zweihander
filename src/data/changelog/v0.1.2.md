Aba de vídeos adicionada para arquivar e reproduzir gravações diretamente no site.

## Adicionado

- Nova página de vídeos com layout `Standalone`
- Grid responsivo de cards: `3 -> 2 -> 1` colunas conforme breakpoints
- Cards com thumbnail, título e data
- Modal de reprodução com `autoPlay`, `loop` e `muted`
