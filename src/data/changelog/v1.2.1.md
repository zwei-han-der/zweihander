Centralização dos estilos do modal, remoção de acoplamento no Blog e ajustes de responsividade em telas pequenas.

## Modificado

- Estilos do modal de changelog/blog movidos para `components.Modal.css`
- Import de `pages.Changelog.css` removido de `Blog.jsx`
- Cover do post no Blog ocultada em `max-width: 480px` para priorizar conteúdo textual

## Corrigido

- Dependência indireta de estilos do modal na página de Blog
- Organização dos estilos responsivos do modal, agora concentrados no arquivo de componente

## Performance

- Configuração de `codeSplitting` reposicionada em `vite.config.js` dentro de `output`
