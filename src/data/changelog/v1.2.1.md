Centralização dos estilos do modal, remoção de acoplamento no Blog e ajuste de responsividade em telas pequenas. Os estilos do modal foram centralizados em um único lugar (`components.Modal.css`) e a listagem do Blog ficou mais limpa em resoluções pequenas.

## Modificado

- Estilos do modal de changelog/blog movidos de `pages.Changelog.css` para `components.Modal.css`
- Import de `pages.Changelog.css` removido de `Blog.jsx`, eliminando acoplamento entre páginas
- Cover do post no Blog passa a ser ocultada em telas pequenas (`max-width: 480px`) para priorizar conteúdo textual

## Corrigido

- Corrigida dependência indireta de estilos do modal na página de Blog
- Corrigida organização dos estilos responsivos do modal, agora concentrados no arquivo de componente

## Performance

- Configuração de `codeSplitting` reposicionada em `vite.config.js` dentro de `output`, alinhando com a estrutura usada no build atual
